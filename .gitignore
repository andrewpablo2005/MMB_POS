# Local environment config (production credentials live only on the server)
conn/config.local.php

# OS junk
.DS_Store
Thumbs.db

# Logs
*.log

# Runtime product photos - uploaded via the app / file manager, never deployed
img/
